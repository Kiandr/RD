diff --git a/GoogleRD/geoMapGoogle.html b/GoogleRD/geoMapGoogle.html
index af19d38..613a98b 100644
--- a/GoogleRD/geoMapGoogle.html
+++ b/GoogleRD/geoMapGoogle.html
@@ -45,14 +45,20 @@
             infoWindow.setContent('Location found.');
             infoWindow.open(map);
             map.setCenter(pos);
+			console.log(pos);
+			alert("[" +position.coords.latitude+ "] is your altitude  position and ["+ position.coords.longitude+"] is your longitude.  Welcome to Nurse Next Door HQ Vancouver");
           }, function() {
             handleLocationError(true, infoWindow, map.getCenter());
           });
+
         } else {
           // Browser doesn't support Geolocation
           handleLocationError(false, infoWindow, map.getCenter());
         }
-      }
+	
+	
+      
+	  }
 
       function handleLocationError(browserHasGeolocation, infoWindow, pos) {
         infoWindow.setPosition(pos);
@@ -60,7 +66,10 @@
                               'Error: The Geolocation service failed.' :
                               'Error: Your browser doesn\'t support geolocation.');
         infoWindow.open(map);
-      }
+      
+	  }
+	  
+	  
     </script>
     <script async defer
     src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDiZN1BV9tlTEYeDD9kQlgeFRnAJwDL5tg&callback=initMap">
